# The cache is regenerated by every run and is not evidence of anything —
# don't push hundreds of cached HTML files (Stage 6 rule).
cache/

# Structured JSON-line logs (retry-like-a-pro extra) — regenerated every run.
logs/

# Change-detection state file — internal plumbing, not run evidence.
output/.previous_hashes.json

# AI-rematch scratch output (kept isolated from the real pipeline's output/).
ai-version/output_ai/
ai-version/output_ai_v2/

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# OS noise
.DS_Store
